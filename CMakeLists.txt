PROJECT(kmodtest)

CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

STRING(STRIP ${KERNEL_SRC} KERNEL_SRC)

MESSAGE(FATAL_ERROR "KERNEL_SRC = ${KERNEL_SRC}")

ADD_SUBDIRECTORY(kmod)
ADD_SUBDIRECTORY(lib)
ADD_SUBDIRECTORY(test)
